import AddTodoButton from './AddTodoButton'

/* ------------------------------------------------------------------- */

export default {
  title: 'Component/AddTodoButton',
  component: AddTodoButton,
}

const Template = (args) => <AddTodoButton {...args} />

export const SmSize = Template.bind({})
